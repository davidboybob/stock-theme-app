# Backend
# (현재 환경변수 불필요 - 네이버 API 인증 없음)
REQUEST_TIMEOUT=10.0

# Frontend (개발 환경)
VITE_API_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000

# Frontend (Docker 환경 - nginx가 프록시하므로 빈 문자열)
# VITE_API_URL=
# VITE_WS_URL=
